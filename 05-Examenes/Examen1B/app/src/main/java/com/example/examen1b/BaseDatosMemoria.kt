package com.example.examen1b

import java.text.SimpleDateFormat

class BaseDatosMemoria {
    /*
    companion object {
        var contadorArtistas = 2
        var contadorCanciones = 5

        val arrayArtista = arrayListOf<Artista>()
        val arrayCancion = arrayListOf<Cancion>()
        val dateFormat = SimpleDateFormat("dd/MM/yyyy")

        init {
            arrayArtista.add(Artista(1, "Jairo", 20, arrayListOf<Cancion>(), true, 100.0))

            arrayCancion.add(Cancion(1, "Cancion1",  1, "Album1", "Genero1", dateFormat.parse("01/01/2001")))
            arrayCancion.add(Cancion(2, "Cancion2",  2, "Album2", "Genero2", dateFormat.parse("02/02/2002")))
            arrayCancion.add(Cancion(3, "Cancion3",  3, "Album3", "Genero3", dateFormat.parse("03/03/2003")))
            arrayCancion.add(Cancion(4, "Cancion4",  4, "Album4", "Genero4", dateFormat.parse("04/04/2004")))

        }

        fun agregarArtista(artista: Artista) {
            artista.id = contadorArtistas++
            arrayArtista.add(artista)
        }

        fun agregarCancion(cancion: Cancion) {
            contadorCanciones++
            arrayCancion.add(cancion)
        }
    }
    */
}